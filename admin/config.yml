backend:
  name: git-gateway
  branch: netlifycms-testing # Branch to update (optional; defaults to master)

media_folder: "assets/images/blog"
logo_url: "/assets/images/LiSEC-LOGO_resized.png"

collections: # A list of collections the CMS should be able to edit
  - name: "post" # Used in routes, ie.: /admin/collections/:slug/edit
    label: "Post" # Used in the UI, ie.: "New Post"
    folder: "_posts" # The path to the folder where the documents are stored
    extension: 'md'
    format: 'yaml-frontmatter' # It should automatically figure these out, but
      # I'll set them to be safe

    sort: "date:desc" # Default is title:asc
    create: true # Allow users to create new documents in this collection
    delete: true # Allow users to delete items
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    format: 'yaml-frontmatter'
    fields: # The fields each document in this collection have
      - {label: "Layout", name: "layout", widget: "hidden", default: "post"}
      - {label: "Title", name: "title", widget: "string", tagname: "h1"}
      - {label: "Description", name: "description", widget: "text", required:
        false, hint: 'Displayed in post previews'}
      - {label: "Image", name: "image", widget: "file", required: false}
      - {label: "Body", name: "body", widget: "markdown"}
    meta: # Meta data fields. Just like fields, but without any preview element
      - {label: "Publish Date", name: "date", widget: "datetime", format: "YYYY-MM-DD hh:mm:ss"}

  - name: "event"
    label: "Event"
    folder: "_events"
    extension: 'md'
    format: 'yaml-frontmatter'
    sort: "date:desc"
    create: true
    delete: true # Allow users to delete items
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Name", name: "title", widget: "string"}
      - {label: "Date", name: "date", widget: "date"}
      - {label: "Link to", name: "link", widget: "string"}
      - {label: "Description", name: "body", widget: "string", required: false}

